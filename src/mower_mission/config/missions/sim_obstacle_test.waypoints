# Sim obstacle test: x y (meters in odom frame). Robot starts at 0,0; path goes forward toward obstacle then right.
# Obstacle in world is at ~3 0; robot should slow (caution) then stop (blocked) or steer.
2 0
6 0
6 2
2 2
2 0
